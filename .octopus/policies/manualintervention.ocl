name = "Debug Project Properties"
description = "Debug to see available project properties"
ViolationReason = "Debug policy"
content = <<-EOT
package manualintervention

default result := {"allowed": false}

# This might error if these don't exist, but worth trying
result := {"allowed": false, "Reason": sprintf("Available: input=%v, data=%v", [object.keys(input), object.keys(data)])} if true
EOT
