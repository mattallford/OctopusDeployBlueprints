name = "Debug Project Properties"
description = "Debug to see available project properties"
ViolationReason = "Debug policy"
content = <<-EOT
package manualintervention

default result := {"allowed": false}

result := {"allowed": false, "Reason": sprintf("Current EnvironmentId: %v, Available environments: %v", [input.EnvironmentId, data.Environments])} if true
EOT
