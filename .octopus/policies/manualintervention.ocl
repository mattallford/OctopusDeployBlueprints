name = "Debug Project Properties"
description = "Debug to see available project properties"
ViolationReason = "Debug policy"
content = <<-EOT
package manualintervention

# Always block but show what's available
default result := {"allowed": false, "Reason": sprintf("Debug - Project data: %v", [input.Project])}

# If input.Project doesn't exist, try this:
# default result := {"allowed": false, "Reason": sprintf("Debug - Full input: %v", [input])}
EOT
