name = "Debug Project Properties"
description = "Debug to see available project properties"
ViolationReason = "Debug policy"
content = <<-EOT
package manualintervention

default result := {"allowed": false}

result := {"allowed": false, "Reason": sprintf("First step data: %v", [input.Steps[0]])} if count(input.Steps) > 0

result := {"allowed": false, "Reason": "No steps found"} if count(input.Steps) == 0
EOT
